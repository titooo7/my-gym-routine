services:
  geminifit:
    build: .
    container_name: geminifit
    restart: always
    ports:
      - "80:80"
    environment:
      # Replace this with your actual API key in a .env file or environment variables
      - API_KEY=${API_KEY}